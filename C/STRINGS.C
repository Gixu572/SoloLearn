#include <stdio.h>
#include <limits.h>

int main()
{
    // char name[10];
    // printf("Enter your name\n");
    // gets(name);
    // puts(name);
    // printf("The name is: %s", name);
    // char info[86];
    // char depth[] = "HR";
    // printf("Enter anything you like \n");
    // sscanf(info, depth, "%s");
    // gets(depth);
    // puts(depth);
    // sscanf(info, "%s", depth);
    // printf("%s\n", depth);
    // int m = INT_MAX;
    // printf("%d",m);
    return 0;
}